// Required by the OpenNext Cloudflare adapter. This is the standard, minimal
// configuration for a Next.js app with no custom caching or overrides needed.
import { defineCloudflareConfig } from '@opennextjs/cloudflare';

export default defineCloudflareConfig();
